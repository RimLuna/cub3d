#ifndef INIT_DEL_H
# define INIT_DEL_H

# include "cub3d.h"


//t_mlx			*mlxdel(t_mlx *mlx);
//t_mlx			*my_init_mlx(void);

void            init_config(t_conf *conf);
void        	zero_vec(t_vec *vector);
void		    init(t_cub *cub);

#endif

